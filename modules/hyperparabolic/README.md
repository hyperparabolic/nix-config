# hyperparabolic

Options starting with `hyperparabolic` are my modules that suitable for import in external repos. The might be defined here, or they may be defined in another repo.

These modules implement options to support global import in a multi-system repo.
