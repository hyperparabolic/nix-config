{
  services.mako = {
    enable = true;
    settings = {
      anchor = "top-center";
      layer = "overlay";
      max-history = 25;

      border-radius = 25;
      outer-margin = "30,0,0,0";
      padding = 15;
    };
  };
  stylix.targets.mako.enable = true;
}
